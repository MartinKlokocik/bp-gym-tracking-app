import SwiftUI

struct WorkoutCompletedView: View {
    var body: some View {
        Text("Workout completed!")
            .font(.headline)
            .navigationBarBackButtonHidden(true)
    }
}

